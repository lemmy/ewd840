CONSTANTS
    N = 7

SPECIFICATION 
    SpecSim

(******************************************************)
(* The order of the (state) constraints below matters *)
(* because some update statistics while others read   *)
(* statistics. We prefix the names with numbers to    *)
(* better indicate it.                                *)
(******************************************************)
CONSTRAINT
    1AtEndOfEachStep
    2AtTermination
    3AtTerminationDetected
    4AtEndOfBehavior
